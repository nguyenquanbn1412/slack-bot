services:
type: web
    name: slack-gpt-bot
    env: python
    buildCommand: ""
    startCommand: "gunicorn main:app"
    envVars:
key: OPENAI_API_KEY
        sync: false
key: SLACK_BOT_TOKEN
        sync: false
